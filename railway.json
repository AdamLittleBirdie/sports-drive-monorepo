{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm --filter shared-types build && pnpm --filter frontend build"
  },
  "deploy": {
    "startCommand": "PORT=${PORT:-3000} ./node_modules/.bin/serve -s apps/frontend/dist -l ${PORT:-3000}"
  }
}
