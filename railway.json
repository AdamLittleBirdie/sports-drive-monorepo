{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm --filter shared-types build && pnpm --filter backend build && pnpm --filter frontend build"
  },
  "deploy": {
    "startCommand": "pnpm --filter frontend preview"
  }
}
