{
  "name": "sports-drive-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "pnpm install",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  }
}
